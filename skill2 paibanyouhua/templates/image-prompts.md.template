# AI 生图提示词
文章标题：__TITLE__
作者：__AUTHOR__
原文链接：__SOURCE_URL__

## 使用原则
- 这份文件是正式交付物，不是几行关键词提纲。
- 每个图项都要写成可直接投喂给中文生图工具的完整提示词，不能只写抽象风格词。
- 现在按 image2 / gpt-image-2 这类强生图模型来写提示词：必须追求高质量、有审美、有完成度的图片，不能再写成“PPT 式单调背景 + 居中大字 + 几个装饰图标”。
- 每张图都必须有具体视觉主体、视觉类型、前景 / 中景 / 背景层次、材质、光线、镜头或视角、空间纵深和审美验收点；标题只是画面版式的一部分，不能替代画面主体。
- 同一篇文章里的封面和正文图要保持统一视觉体系，但至少混合 2 种视觉类型，例如杂志级信息图、场景摄影感、3D 静物装置、数据新闻图、编辑部插画、拼贴海报，避免每张都长成同一种深蓝信息卡。
- 只允许使用文章里已经确认过的真实信息、真实数据、真实来源，不得为了好看补造数字、机构名或结论。
- 需要中文的图，必须明确要求只使用简体中文，文字自然、准确、清晰可读，不允许乱码、错别字、异体字、繁简混用、拼音代字。
- 如果模型中文稳定性一般，也要给出后期覆字方案、安全留白区域和需要人工覆盖的文案清单，但这只是兜底，不是主方案。
- 用 API 生图时必须使用本文件中对应槽位的提示词；如果要改提示词，必须先更新本文件，再用更新后的提示词调用 API。禁止临时绕过本文件手写一段新 prompt 直接生图。

## 槽位约定
- 横版封面：`assets/cover-wide.jpg`
- 方形封面：`assets/cover-square.jpg`
- 正文配图 1：`assets/img-01.jpg`
- 正文配图 2：`assets/img-02.jpg`
- 正文配图 3：`assets/img-03.jpg`
- 正文配图 4：`assets/img-04.jpg`

## 必带硬约束

### 提示词可执行性预检
每个槽位调用 API 前都必须确认以下项目已经写清楚：画面类型、具体主体、主体占比、前景 / 中景 / 背景、构图比例、标题区、数据区、留白区、文字清单、数据清单、视觉类型、材质、光线、镜头或视角、失败反例、手机端可读性。任意一项缺失，先补全提示词，不得调用 API。

### 审美质量模板
```text
这张图必须像经过认真设计的中文商业媒体视觉作品，有清楚的视觉主体、前中背景层次、真实材质或明确插画语言、克制但有记忆点的光影与色彩；不能像 PPT 模板，不能只是单调背景加大字，不能像临时占位图。
```

### 反 PPT 模板
```text
注意：画面不能只是纯色或渐变背景加一个巨大标题；标题只是版式的一部分，必须有可见的主体、场景、物件、数据结构或视觉隐喻。请避免 PPT 封面感、大字报感、空背景感和廉价模板感。
```

### 中文正确性模板
```text
画面中的所有文字仅使用简体中文，中文必须自然、准确、完整、清晰可读，不允许出现乱码、错别字、异体字、繁简混用、拼音代字、无意义符号、残缺笔画。
```

### 数据正确性模板
```text
图中的数字、年份、百分比、标签名称、来源标注都必须与给定数据完全一致，不要擅自改写，不要省略关键单位，不要生成与正文不一致的虚构数据。
```

### 负向提示词模板
```text
不要 PPT 模板感，不要单调渐变背景，不要纯色背景加巨大标题，不要空白背景，不要廉价科技感光效，不要乱码中文，不要错别字，不要伪汉字，不要繁简混用，不要英文字母乱入，不要密集小字，不要低清晰度，不要脏背景，不要错误图表比例，不要虚构数据，不要无意义图标堆砌。
```

## 封面创意

### 封面创意 A：观点冲击型
- 横版槽位编号：cover-wide
- 横版目标文件名：assets/cover-wide.jpg
- 横版比例：2.35:1
- 方形槽位编号：cover-square
- 方形目标文件名：assets/cover-square.jpg
- 方形比例：1:1
- 用途：公众号默认封面 + 后续分发备用方封面
- 适合原因：
- 画面核心：
- 视觉类型：
- 主体与审美钩子：
- 必须出现的中文：
  - 主标题：
  - 副标题 / 角标：
- 必须出现的数据：
  - 数据 1：
  - 数据 2：
- 横版构图：
- 方形构图：
- 画面层次：
- 版式：
- 色调与材质：
- 光线、材质与镜头：
- 中文排版要求：
- 审美验收点：
- 横版正向提示词：
  ```text
  在这里写一整段超详细中文提示词，必须包含 2.35:1 横版构图、具体主体、视觉类型、前景/中景/背景、主体占比、材质、纹理、光线、镜头或视角、中文文案、关键数据、来源位置、版式层级，并附带“审美质量模板”“反 PPT 模板”“中文正确性模板”和“数据正确性模板”。
  ```
- 方形正向提示词：
  ```text
  在这里写一整段超详细中文提示词，必须包含 1:1 方形构图、主体重排、前景/中景/背景、留白区域、标题位置、关键数据位置、材质、光线、镜头或视角，并附带“审美质量模板”“反 PPT 模板”“中文正确性模板”和“数据正确性模板”。
  ```
- 负向提示词：
  ```text
  在这里按该封面实际需求补全负向提示词，并至少覆盖“负向提示词模板”的核心约束。
  ```
- 后期覆字说明：

### 封面创意 B：场景氛围型
- 横版槽位编号：cover-wide
- 横版目标文件名：assets/cover-wide.jpg
- 横版比例：2.35:1
- 方形槽位编号：cover-square
- 方形目标文件名：assets/cover-square.jpg
- 方形比例：1:1
- 用途：公众号默认封面 + 后续分发备用方封面
- 适合原因：
- 画面核心：
- 视觉类型：
- 主体与审美钩子：
- 必须出现的中文：
  - 主标题：
  - 副标题 / 角标：
- 必须出现的数据：
  - 数据 1：
  - 数据 2：
- 横版构图：
- 方形构图：
- 画面层次：
- 版式：
- 色调与材质：
- 光线、材质与镜头：
- 中文排版要求：
- 审美验收点：
- 横版正向提示词：
  ```text
  在这里写一整段超详细中文提示词，突出具体场景、情绪、人物或物体关系，写清前景/中景/背景、光线、材质、镜头或视角，同时明确中文文案和数据的呈现方式，并附带“审美质量模板”和“反 PPT 模板”。
  ```
- 方形正向提示词：
  ```text
  在这里写一整段超详细中文提示词，说明 1:1 方构图下主体、文案和数据如何重排，保留完整视觉主体和安全留白，不能机械裁切横版。
  ```
- 负向提示词：
  ```text
  在这里补全该方案专属负向提示词。
  ```
- 后期覆字说明：

### 封面创意 C：信息数据型
- 横版槽位编号：cover-wide
- 横版目标文件名：assets/cover-wide.jpg
- 横版比例：2.35:1
- 方形槽位编号：cover-square
- 方形目标文件名：assets/cover-square.jpg
- 方形比例：1:1
- 用途：公众号默认封面 + 后续分发备用方封面
- 适合原因：
- 画面核心：
- 视觉类型：
- 主体与审美钩子：
- 必须出现的中文：
  - 主标题：
  - 副标题 / 角标：
- 必须出现的数据：
  - 数据 1：
  - 数据 2：
  - 数据 3：
- 横版构图：
- 方形构图：
- 画面层次：
- 版式：
- 色调与材质：
- 光线、材质与镜头：
- 中文排版要求：
- 审美验收点：
- 横版正向提示词：
  ```text
  在这里写一整段超详细中文提示词，说明卡片、图表、时间轴或数字模块怎么排，重点数字放哪里，来源标注放哪里；同时写清具体视觉主体、空间层次、材质、光线和反 PPT 约束，不能只是背景加数据大字。
  ```
- 方形正向提示词：
  ```text
  在这里写一整段超详细中文提示词，说明同一套视觉主题在 1:1 版本下如何保持信息清晰、主体完整、层级明确，并避免 PPT 卡片堆叠感。
  ```
- 负向提示词：
  ```text
  在这里补全该方案专属负向提示词。
  ```
- 后期覆字说明：

## 正文配图

### 配图 1：位于《对应小标题》后
- 槽位编号：img-01
- 目标文件名：assets/img-01.jpg
- 插入位置：写明放在第几个 H2 或哪一段后
- 对应段落：
- Markdown 引用：`![配图 1：这里写 alt 文案](img-01.jpg)`
- alt 文案：
- 图例说明：
- 配图目的：信息强化 / 数据说明 / 节奏调节 / 场景还原
- 对应内容：
- 画面核心：
- 视觉类型：
- 主体与审美钩子：
- 必须出现的数据：
  - 指标 1：
  - 指标 2：
- 必须出现的中文：
  - 主标签：
  - 次标签：
  - 来源：
- 结构与构图：
- 色调与风格：
- 光线、材质与镜头：
- 中文排版要求：
- 审美验收点：
- 正向提示词：
  ```text
  在这里写一整段超详细中文提示词，必须包含具体主体、视觉类型、前景/中景/背景、主体占比、图表或场景结构、中文标签、关键数字、来源位置、镜头或视角、材质、纹理、光线、版式、留白说明，并附带“审美质量模板”和“反 PPT 模板”。
  ```
- 负向提示词：
  ```text
  在这里补全该配图专属负向提示词。
  ```
- 免费图库备选关键词：
- 后期覆字说明：

### 配图 2：位于《对应小标题》后
- 槽位编号：img-02
- 目标文件名：assets/img-02.jpg
- 插入位置：
- 对应段落：
- Markdown 引用：`![配图 2：这里写 alt 文案](img-02.jpg)`
- alt 文案：
- 图例说明：
- 配图目的：
- 对应内容：
- 画面核心：
- 视觉类型：
- 主体与审美钩子：
- 必须出现的数据：
  - 指标 1：
  - 指标 2：
- 必须出现的中文：
  - 主标签：
  - 次标签：
  - 来源：
- 结构与构图：
- 色调与风格：
- 光线、材质与镜头：
- 中文排版要求：
- 审美验收点：
- 正向提示词：
  ```text
  在这里写一整段超详细中文提示词，必须包含具体主体、视觉类型、前景/中景/背景、材质、光线、镜头或视角、中文文案、关键数据、版式层级和反 PPT 约束。
  ```
- 负向提示词：
  ```text
  在这里补全该配图专属负向提示词。
  ```
- 免费图库备选关键词：
- 后期覆字说明：

### 配图 3：位于《对应小标题》后
- 槽位编号：img-03
- 目标文件名：assets/img-03.jpg
- 插入位置：
- 对应段落：
- Markdown 引用：`![配图 3：这里写 alt 文案](img-03.jpg)`
- alt 文案：
- 图例说明：
- 配图目的：
- 对应内容：
- 画面核心：
- 视觉类型：
- 主体与审美钩子：
- 必须出现的数据：
  - 指标 1：
  - 指标 2：
- 必须出现的中文：
  - 主标签：
  - 次标签：
  - 来源：
- 结构与构图：
- 色调与风格：
- 光线、材质与镜头：
- 中文排版要求：
- 审美验收点：
- 正向提示词：
  ```text
  在这里写一整段超详细中文提示词，必须包含具体主体、视觉类型、前景/中景/背景、材质、光线、镜头或视角、中文文案、关键数据、版式层级和反 PPT 约束。
  ```
- 负向提示词：
  ```text
  在这里补全该配图专属负向提示词。
  ```
- 免费图库备选关键词：
- 后期覆字说明：

### 配图 4：位于《对应小标题》后
- 槽位编号：img-04
- 目标文件名：assets/img-04.jpg
- 插入位置：
- 对应段落：
- Markdown 引用：`![配图 4：这里写 alt 文案](img-04.jpg)`
- alt 文案：
- 图例说明：
- 配图目的：
- 对应内容：
- 画面核心：
- 视觉类型：
- 主体与审美钩子：
- 必须出现的数据：
  - 指标 1：
  - 指标 2：
- 必须出现的中文：
  - 主标签：
  - 次标签：
  - 来源：
- 结构与构图：
- 色调与风格：
- 光线、材质与镜头：
- 中文排版要求：
- 审美验收点：
- 正向提示词：
  ```text
  在这里写一整段超详细中文提示词，必须包含具体主体、视觉类型、前景/中景/背景、材质、光线、镜头或视角、中文文案、关键数据、版式层级和反 PPT 约束。
  ```
- 负向提示词：
  ```text
  在这里补全该配图专属负向提示词。
  ```
- 免费图库备选关键词：
- 后期覆字说明：
